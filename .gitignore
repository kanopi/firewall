vendor
.idea
.vscode
composer.lock
reports
.phpunit.result.cache
.phpunit.cache
phpcs-report.xml
phpstan-report.xml
rector-report.xml
firewall.zip
.env
.env.local
.env.*.local
coverage-report

# crs-engine is developed in-tree but is its own composer package.
# Extract to a standalone repo before release.
/crs-engine/

# Documentation site: `mkdocs build` output and the local preview virtualenv.
# The published site lives on the gh-pages branch, built by CI.
/site/
.venv-docs/