vendor
.idea
.vscode
composer.lock
reports
.phpunit.result.cache
.phpunit.cache
phpcs-report.xml
phpstan-report.xml
rector-report.xml
firewall.zip
.env
.env.local
.env.*.local
coverage-report

# crs-engine is developed in-tree but is its own composer package.
# Extract to a standalone repo before release.
/crs-engine/

# Documentation site: `mkdocs build` output and the local preview virtualenv.
# The published site lives on the gh-pages branch, built by CI.
/site/
.venv-docs/

# Local scratch: debugging scaffolds and working notes that live alongside a
# checkout but are nobody else's business. Root-anchored so a legitimately
# committed config of the same name elsewhere in the tree is unaffected.
/check-challenge-page.php
/diagnose-altcha.php
/firewall.yml
/logs/
/release.md
